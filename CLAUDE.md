# CLAUDE.md

## Permissions

- All bash commands are allowed
- All git commands are allowed (commit, push, pull, rebase, reset, etc.)
- All gh CLI commands are allowed (pr create, issue create, repo, etc.)
